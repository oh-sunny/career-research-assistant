# AGENTS.md — 산업·직무 인사이트 수집 오케스트레이션

이 프로젝트에서 메인 에이전트는 실행 순서를 관리하고 외부 저장과 전송을 한 번만 수행한다. 서브에이전트는 조사·검수·브리핑 작성처럼 읽기 중심 작업만 담당한다.

## 사용할 서브에이전트

- `hr_researcher`: HR 자료 조사, 기본 목표 최대 2건, 요청 건수 우선
- `md_researcher`: MD 자료 조사, 기본 목표 최대 2건, 요청 건수 우선
- `url_analyzer`: 사용자가 제공한 URL의 원문 분석, 한 번에 최대 5건
- `result_reviewer`: 새로 탐색한 조사 결과만 통합 검수, 직접 링크에는 사용하지 않음
- `briefing_writer`: Slack 전송을 명시적으로 요청한 경우에만 저장 확인 자료의 초안 작성

## 요청형 수집 실행 순서

사용자 요청으로 시작한다. 건수를 지정하면 그대로 목표로 전달하고, 건수가 없으면 요청한 직무별 최대 2건, 직무도 없으면 HR 2건·MD 2건을 기본 목표로 한다. `요약만` 요청에서는 결과 표시까지만 수행하고 Notion·로컬 기록·Slack·라이브러리를 변경하지 않는다.

1. 메인 에이전트가 `data/archive-index.jsonl`을 확인하고 직무·업무 영역별 축적 현황을 두 조사 에이전트에게 전달한다.
2. 요청한 직무의 조사 담당자만 실행한다. HR·MD 모두 요청했으면 병렬로 실행한다.
3. 요청한 조사가 끝난 뒤 `result_reviewer`를 실행한다. 한 조사만 실패했다면 성공한 결과는 검수하되 실패를 실행 기록에 남긴다. 직접 링크 결과는 전달하지 않는다.
4. 메인 에이전트만 `pass` 자료를 Notion에 한 건씩 저장하고 생성된 각 페이지만 다시 읽어 확인한다.
5. 저장과 재확인에 성공한 자료만 `data/archive-index.jsonl`에 추가한다. 선정·제외 판단과 실행 결과는 기존 기록 기준에 맞게 한 번만 기록한다.
6. 저장된 제목과 Notion 링크, 직무별 저장 건수·부족 건수와 이유를 짧게 보고한다. 같은 자료를 두 건으로 중복 저장하지 않는다.
7. Slack 전송을 명시적으로 요청한 경우에만 저장 성공 자료를 `briefing_writer`에게 전달하고, 메인 에이전트가 `scripts/slack_sender.py`로 한 번 전송한다.
8. 웹 라이브러리 갱신은 명시적으로 요청한 경우에만 수행한다. 평소 저장 완료 조건에 포함하지 않는다.

## 사용자 URL 실행 순서

1. 메인 에이전트가 입력된 URL을 최대 5개까지 받아 로컬 인덱스에서 중복을 먼저 확인한다.
2. 중복이 아닌 URL과 재요약을 요청한 중복 URL을 `url_analyzer`에 전달한다. URL 외의 새 자료는 사용자가 함께 요청했을 때만 탐색한다.
3. `url_analyzer`는 본문 접근과 사실 요약만 수행한다. A/B/C 등급·최신성·자료 유형으로 저장을 거절하지 않고 `result_reviewer`도 호출하지 않는다.
4. 사용자가 `요약만` 또는 재요약만 요청했으면 결과만 보여주고 Notion·로컬 기록·Slack·라이브러리를 변경하지 않는다. 그 외에는 본문을 읽어 요약한 자료를 메인 에이전트가 기존 DB에 저장·재확인하고 영구 인덱스에 추가한다. 관련성을 설명할 수 없는 분류·본문 영역은 생략한다.
5. 저장된 제목과 Notion 링크, 중복·접근 실패·저장 실패를 짧게 알린다. 접근 실패는 본문 붙여넣기 대안을 안내하며, 사용자에게 처리 상태를 관리하게 하지 않는다.
6. Slack 전송과 라이브러리 갱신은 각각 명시적으로 요청했을 때만 수행한다. 링크와 수집을 함께 요청하면 링크부터 처리하고 탐색 결과만 검수한다.

최대 5개를 초과한 직접 링크는 남은 수를 알리고 다음 묶음으로 안내한다. 서로 다른 직접 링크는 같은 주제라는 이유로 제외하지 않으며, 중복 URL은 기존 Notion 링크를 안내한다.

## 단일 작성자 원칙

- 서브에이전트는 프로젝트 파일, Notion, Slack을 변경하지 않는다.
- 메인 에이전트만 Notion 저장, 로컬 JSONL 갱신과 Slack 전송을 수행한다.
- Slack 비밀 인증값과 채널 ID는 서브에이전트 프롬프트나 결과에 포함하지 않는다.
- HR·MD 조사 결과가 같은 사건이면 대표 자료 한 건만 저장한다.
- Notion 저장에 실패한 자료는 영구 인덱스와 Slack PICK에 넣지 않는다.
- Slack 실패는 이미 저장된 Notion 자료를 되돌리지 않는다.
- 통신 시간초과처럼 전송 여부가 불확실하면 무조건 재전송하지 않고 확인 필요로 기록한다.
- Notion 생성 여부나 생성 후 읽기 확인이 불확실하면 반환된 페이지 ID·URL을 실행 기록에 남기고 해당 페이지부터 확인한다. 확인 전 새 페이지를 반복 생성하지 않는다.

## 실행 범위

- 요청형 수집은 검수·중복 제외 후의 저장 건수를 목표로 하되 기준을 통과한 자료가 부족하면 이유를 보고하고 종료한다.
- 탐색하는 `산업·기업 동향`은 실행일 기준 최근 1년 이내 자료만 사용한다. 직접 링크에는 이 선별 제한을 적용하지 않는다.
- `직무 공부`는 기간보다 실제 업무, 판단, 협업과 개선 과정의 깊이를 우선한다.
- 서브에이전트 설정만으로 예약 실행이 생기지는 않는다. 예약은 별도 자동화가 이 전체 흐름을 호출할 때만 동작한다.
- 예약 작업이 호출해도 Slack·라이브러리는 해당 작업에 명시적으로 포함된 경우에만 실행한다. 이번 스킬 개정은 예약 작업이나 별도 링크 입력함을 생성하지 않는다.

에이전트 사이의 입력·출력 형식과 실패 처리는 `specs/subagent-workflow.md`를 따른다.
